# Complete early cup source coverage — 11 September 2026

The final coverage audit found that the initial 38-country review omitted **266 stored main-cup rows in 50 countries** through global season 24. Of those, **216 rows in 42 countries** precede global season 24. Every omitted record had `finalMatchId=0`. There were no unreviewed early stored seasons within the original 38 countries' screening ranges.

All **266 omitted rows now have independently captured public national-cup winner facts**, with exact displayed local seasons. Together with the original reviews and recovered-gap evidence, this closes public-source coverage for **all 619 stored main-cup rows across 88 countries through global season 24**. The complete key-by-key coverage check reports **zero unsourced records**; name agreement alone is not proof against a defective source.

The independent scope calculation uses the committed `server/src/data/leagues.json` snapshot: its maximum country season is 94, so each country's global offset is 94 minus its local current season in that same snapshot. Every main-cup and national-league catalog season matched the seed. The captured [Belgium Cup](https://wiki.hattrick.org/wiki/Belgium_Cup) transition is local 10 to 11 with offset 13, while [Copa do Brasil](https://wiki.hattrick.org/wiki/Copa_do_Brasil) changes from two legs to one between local 11 and 12 with offset 12. Both place the transition at global season 24. The audit retains global 24 as a conservative extra boundary season and preserves historical displayed season numbering, including restarted countries.

The additional public comparison initially found **241 normalized name agreements, four HTML entity equivalents, and 21 other name differences**. Full before-change database rows and sources are retained in `qa/cup-remaining-wiki-comparison-before-fixes.json`. Names, title-count suffixes and source table columns were reviewed explicitly; no numeric account or team identity was inferred from a name. HTML equivalents are Lithuania seasons 2 and 5 and Czechia seasons 2 and 3. The other differences are candidates until primary aggregate evidence establishes a different club, because historical renames can produce name differences too.

The coordinating primary check completed guarded CHPP `cupmatches` final and preceding rounds for all 266 reconstructed rows, without `matchdetails` calls or stored match re-fetches. It confirmed **20 additional wrong finalists**; all 20 primary winners agree with the independently captured public tables. The frozen complete evidence is `qa/expanded-cup-primary-evidence.json`, SHA-256 `735ac6d3d542af28abf5608d350cbecfe3303e101000a2d4ee34505d357abb7b`. Offline primary replay agreed for all 266 rows: 265 decisive primary results and one explicit unresolved participant-name case. For the South Korea season-6 participant-name ambiguity, the [Hanguk National Trophies table](https://wiki.hattrick.org/wiki/Hanguk) independently confirms the stored winner Busan power, managed by hattrick2003.

The other public discrepancy is Czechia cup 50, season 1. The [Czechia National Trophies table](https://wiki.hattrick.org/wiki/Czechia) names Ničivá Síla. Both archived final legs instead name `hasek` and `FC Mike`: 8–1 in the final listed round and 9–1 from hasek's perspective in the preceding round, proving **hasek 17–2** on aggregate. The club wiki link has no article, and no explicit rename evidence was found. The stored historical name remains hasek; this is recorded as a source discrepancy without asserting a rename or an owner.

After the coordinator applied all 20 corrections, the final independent regression passed: **619 winners checked, zero missing sources and zero unexplained winner mismatches**. Of these, 616 agree with published names after HTML-entity and whitespace normalization only; three rely on independently re-derived primary results for documented source errors: Egypt 19/S6, Iceland 36/S10 and Czechia 50/S1. The fourth known source error, Iceland 36/S12, is global season 25 and outside the 619-row scope; its exact captured primary evidence is still validated by the regression. The source exceptions require the actual score-derived winner and cannot allow an arbitrary replacement. Czechia's frozen evidence checksum is also verified. The initial wrong-winner run failed before the repair, and the same correctness assertions now pass.

The additional 266-row cached comparison now reports 261 exact normalized name agreements, four HTML entity equivalents and the one explained Czechia source discrepancy. Its before-fix report remains unchanged.

The reproducible files are:

- `qa/audit-cup-era-coverage.mjs`: read-only scope calculation; exact omitted rows in `qa/cup-two-leg-coverage-gaps.json`.
- `qa/wiki-stored-audit/remaining-national-trophy-rows.json`: all 266 newly reviewed season/winner facts, country, cup ID, source URL and source line.
- `qa/compare-remaining-cup-wiki.mjs`: read-only comparison against the current database; current result `qa/cup-remaining-wiki-comparison.json`.
- `qa/verify-complete-early-cup-source-coverage.mjs`: checks the exact additional source key set, duplicates, source presence and actual winner correctness for all 619 early stored rows; result `qa/cup-complete-early-source-coverage.json`. A missing source, wrong winner or invalid exception evidence exits nonzero.

All four scripts/artifacts are local QA outputs. This independent source-review task changed no production database rows, no baked JSON and no API response or parser code.
